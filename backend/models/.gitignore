# Trained model binaries — regenerate with:
#   python models/collect_dataset.py  (requires gee-key.json)
#   python models/train_model.py
# *.pkl

# Raw dataset CSV (also regenerated by collect_dataset.py)
uhi_dataset.csv
